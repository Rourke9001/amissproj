# Test-only config exercising Config.actionCosts() precedence (ConfigActionCostsTest):
# one valid cost override, and one malformed value that must fall back to the default.
# No db.* keys here - the Config database getters keep their built-in defaults.
costs.work-minutes=300
costs.eat-minutes=not-a-number
