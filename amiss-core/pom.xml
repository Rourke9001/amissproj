<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.rourke9001</groupId>
        <artifactId>amissproj</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>amiss-core</artifactId>
    <packaging>jar</packaging>

    <name>AmissProj :: core</name>
    <description>Game rules: domain model + board, application services behind
        repository ports, Flyway migration sources. UI- and Spring-free —
        consumed by the amiss-api REST module.</description>

    <dependencies>
        <!-- MySQL JDBC driver (Community build from Maven Central). -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

        <!--
          Flyway versioned schema migrations (classpath:db/migration), applied at
          API startup by Spring Boot's auto-configured Flyway as the amiss_migrator
          user (the runtime amiss user has no DDL rights). The SQL lives here so the
          rules and their schema version together; flyway-mysql is the MySQL support
          module. amiss-api takes flyway + the JDBC driver transitively from this pom.
        -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!--
          Logging: core only speaks the SLF4J facade; Spring Boot brings the
          backend in amiss-api. (Test scope below still needs a real backend so
          logback-test.xml can silence the deliberate SQLException fallbacks.)
        -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- BCrypt password hashing. -->
        <dependency>
            <groupId>org.mindrot</groupId>
            <artifactId>jbcrypt</artifactId>
        </dependency>

        <!-- ===== Test scope ===== -->
        <!-- JUnit 5 (Jupiter aggregator: API + params + engine), version from the BOM. -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
          Mockito mocks the application.port interfaces so the service rules are
          unit-tested in isolation from MySQL. mockito-junit-jupiter provides
          MockitoExtension.
        -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Backend for src/test/resources/logback-test.xml (see logging note). -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              Flyway CLI-style goals (flyway:migrate / flyway:info) for CI and
              local ops, pointed straight at the migration sources so no compile
              is needed. Connection settings come from -Dflyway.url/user/password.
              The MySQL support module + JDBC driver must be on the *plugin*
              classpath, hence the plugin-level dependencies.
            -->
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <configuration>
                    <locations>
                        <location>filesystem:src/main/resources/db/migration</location>
                    </locations>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.flywaydb</groupId>
                        <artifactId>flyway-mysql</artifactId>
                        <version>${flyway.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>com.mysql</groupId>
                        <artifactId>mysql-connector-j</artifactId>
                        <version>${mysql.connector.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
</project>
