<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback configuration for AmissProj.

  Bundled to the classpath root by scripts\build.ps1 (it copies every non-.java
  file under src/ into build/classes, preserving layout). SLF4J/Logback then
  picks it up automatically at startup.

  Two appenders: a human-readable console log and a rolling file at
  logs/amiss.log (gitignored). Override the level without rebuilding by passing
  -Dlogback.configurationFile=... or editing this file and re-running build.ps1.
-->
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{24} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/amiss.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/amiss.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
