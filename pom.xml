<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.rourke9001</groupId>
    <artifactId>amissproj</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>AmissProj</name>
    <description>Remake of "Jones in the Fast Lane", persisted in MySQL: game rules
        (amiss-core), the Swing desktop client (amiss-swing) and a Spring Boot REST
        API (amiss-api).</description>

    <!--
      Reactor layout (ROADMAP Phase 3 / KAN-27):
        amiss-core      game rules: domain + application + infrastructure, Flyway
                        migrations, the whole unit-test suite. No UI dependencies.
        amiss-swing     the original Swing desktop client; builds the shaded
                        runnable AmissProj.jar.
        amiss-api       Spring Boot REST API over the same core (KAN-16).
        amiss-coverage  aggregates the per-module JaCoCo reports for CI.
    -->
    <modules>
        <module>amiss-core</module>
        <module>amiss-swing</module>
        <module>amiss-api</module>
        <module>amiss-coverage</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--
          Java 21 LTS across the reactor: Spring Boot 3 needs 17+, CI already runs
          Temurin 21, and one release number keeps the build easy to reason about.
          (Until Phase 3 the sources targeted 8 on the grounds of "don't touch what
          works"; the API module forced the decision.)
        -->
        <maven.compiler.release>21</maven.compiler.release>

        <mysql.connector.version>9.7.0</mysql.connector.version>
        <flyway.version>11.8.2</flyway.version>
        <!--
          SLF4J/Logback aligned with the versions Spring Boot 3.5 manages, so the
          API module never sees a conflicting logging stack via nearest-wins.
        -->
        <slf4j.version>2.0.17</slf4j.version>
        <logback.version>1.5.18</logback.version>
        <jbcrypt.version>0.4</jbcrypt.version>

        <!-- Test toolchain. -->
        <junit.version>5.10.2</junit.version>
        <mockito.version>5.11.0</mockito.version>
        <surefire.version>3.2.5</surefire.version>
        <jacoco.version>0.8.12</jacoco.version>
    </properties>

    <!--
      One place for every third-party version; modules declare dependencies
      version-less. JUnit artifacts (jupiter API/params/engine, platform) are
      aligned via the official BOM.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql.connector.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-mysql</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>${logback.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mindrot</groupId>
                <artifactId>jbcrypt</artifactId>
                <version>${jbcrypt.version}</version>
            </dependency>
            <dependency>
                <groupId>org.netbeans.lib</groupId>
                <artifactId>absolutelayout</artifactId>
                <version>1.0.0</version>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-junit-jupiter</artifactId>
                <version>${mockito.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <!-- Keep method parameter names in the bytecode: Spring MVC resolves
                             @PathVariable/@RequestParam names reflectively (the Boot parent
                             would set this, but this build imports only the BOM). -->
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>3.6.0</version>
                </plugin>
                <plugin>
                    <groupId>org.flywaydb</groupId>
                    <artifactId>flyway-maven-plugin</artifactId>
                    <version>${flyway.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!--
              Pin Surefire 3.x in every module so the JUnit 5 (JUnit Platform)
              engine is discovered and the test run is reproducible across Maven
              versions / CI runners.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
            </plugin>

            <!--
              JaCoCo per-module coverage. prepare-agent wires the agent into the
              Surefire JVM; each module writes its own target/jacoco.exec, and the
              amiss-coverage module aggregates them for CI.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
